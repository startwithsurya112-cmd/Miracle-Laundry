# Server Configuration
PORT=5000
NODE_ENV=development

# MongoDB Connection String (Atlas or Local)
# Replace with your MongoDB connection string:
MONGODB_URI=mongodb://127.0.0.1:27017/miraclelaundry
# Example for MongoDB Atlas:
# MONGODB_URI=mongodb+srv://<username>:<password>@cluster0.xxxxx.mongodb.net/miraclelaundry?retryWrites=true&w=majority

# JWT Authentication Secret Key
JWT_SECRET=miracle_laundry_super_secure_jwt_secret_key_2026

# WhatsApp Gateway / Webhook Config (Optional)
WHATSAPP_ENABLED=true
